#include <iostream>
using namespace std;

int main(){
    //replacing if else statement we use ternary operator

    // int grade = 80;
    // grade <=50 ? cout << "Poor grade " : cout << "distinction";

    // int num = 4;
    // num % 2 ? cout <<"odd" : cout<< "even";

    bool friends = false ;
    // friends ? cout << "come in" : cout << "Get lost";
    // this can be written in one more format 

    cout << (friends ? "come in" : "get lost" );
    return 0;
}